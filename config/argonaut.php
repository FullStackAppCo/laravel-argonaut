<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Store definitions
    |--------------------------------------------------------------------------
    |
    | Each entry defines the location of a JSON store which may be accessed using
    | the name corresponding with the array key e.g. Argonaut::store('theme').
    | Each entry must specify the name of a disk configured in the filesystems
    | configuration file and a relative path on the disk where the JSON file
    | will be stored.
    |
    */
    'stores' => [
//        'theme' => [
//            'disk' => 'local',
//            'path' => 'settings/theme.json',
//        ],
    ],

];